import 'package:flutter/material.dart';

class PointHistoryScreen1 extends StatelessWidget {
  const PointHistoryScreen1({super.key});
  //  title: const Text('Lecture3.2 Part2 Screen'),
  //   centerTitle: false,
  //   leading: const Icon(Icons.menu),
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Онооны тү'),
      ),
      body: Column(
        children: [Text('This is Point History')],
      ),
    );
  }
}
